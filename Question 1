#include  <iostream>
using namespace std;
bool isleapYear(int year)
{
    return (((year %4 == 0) && (year % 100 != 0)) ||
        (year % 400 == 0));
}
int getCenturyValue(int year)
{
    int t, rem;
    t=
